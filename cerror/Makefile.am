
# In the below, we will always build a 'cval' default binary.  Additionally, if
# we can build a 32 bit verson and 64 bit version, build them with appropriate
# suffixes.

bin_PROGRAMS = cerror
cerror_SOURCES = cerror.c
